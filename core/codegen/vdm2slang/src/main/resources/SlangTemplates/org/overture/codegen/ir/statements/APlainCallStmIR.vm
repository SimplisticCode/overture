##Example: A`foo(1, 1)
##
#set( $class = "" )
##
#if (!$SlangFormat.isNull($node.getClassType()))
	#set( $separator = ".")
	#set( $classType = "$JavaFormat.format($node.getClassType())")
	#set( $class = "$classType$separator")
#end
##
#set( $return = "")
#if (!$SlangFormat.isVoidType($node.getType()))
	#set( $return = "return ")
#end
##
#set( $name = "$node.getName()")
##
$return$class$name($SlangFormat.formatArgs($node.getArgs()))