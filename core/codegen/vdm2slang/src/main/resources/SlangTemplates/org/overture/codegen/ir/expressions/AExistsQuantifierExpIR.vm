#set( $set = $SlangFormat.formatSet($node.getBindList()))
#set( $predVar = $SlangFormat.formatPredicateVar($node.getBindList()))
##
(${set}.elements.filter(${predVar} => $SlangFormat.format($node.getPredicate()))).nonEmpty
