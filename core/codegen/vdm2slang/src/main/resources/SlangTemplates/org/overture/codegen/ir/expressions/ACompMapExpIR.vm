#set( $set = $SlangFormat.formatSet($node.getBindings()))
#set( $predVar = $SlangFormat.formatPredicateVar($node.getBindings()))
##
Map.empty ++ (for (${predVar} <- ${set}.elements.filter(${predVar} => $SlangFormat.format($node.getPredicate()))) yield ($SlangFormat.formatMapping($node.getFirst()))).elements