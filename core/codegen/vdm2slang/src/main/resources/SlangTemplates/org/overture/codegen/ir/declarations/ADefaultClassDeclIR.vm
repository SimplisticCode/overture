## TODO: handle 'functions', 'operations' and 'traces'
class $node.getName():
#if ( $node.getFields().isEmpty() )
    pass
#else
#**##foreach( $f in $node.getFields() )
#**#  $SlangFormat.format($f)
#**##end
#end
