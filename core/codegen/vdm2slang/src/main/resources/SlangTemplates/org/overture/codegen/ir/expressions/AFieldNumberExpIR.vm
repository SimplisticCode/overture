#set( $type = $SlangFormat.formatTemplateParam($node.getType()) )
#set( $index = $node.getField() - 1 )
##
#set( $tuple = $SlangFormat.format($node.getTuple()) )
#set( $method = ".get($index)" )
##
#set( $field = "")
#if ($SlangFormat.isSeqConversion($node))
	#set( $field = "${tuple}${method}")##
	${field}##
#else
	#set( $field = "((${type}) ${tuple}${method})")##
	#if ($ValueSemantics.cloneMember($node))
		Utils.copy(${field})##
	#else
		${field}##
	#end
#end
##