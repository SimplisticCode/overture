class Entry

operations

public static Run : () ==> set of nat
Run () ==
(
	dcl s : set of nat := {2,4,6};
	
	while forall x in set s & x mod 2 = 0 do
	(
		s := s union {3};
	);
	
	return s;
);

end Entry
